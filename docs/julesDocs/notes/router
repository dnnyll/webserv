ROUTER STEP

ETAPE 1 "pre routage"

1 extraction de l'uri demande
2 parcourir serverblock pour trouver la location la plus longue correspondante
//3 est ce que ce block accepte la methode demande ?
//	est ce que le content length depasse la client max body size?
//	-> renvoyer lerreur correspondante
4 definir le vrai chemin a partir de root et de l'uri

ETAPE 2 "application du schema"
1 test du chemin avec (stat)
	si non avec get delete -> response 404 not found
	si non avec post -> est ce que le dossier parent accepte l'ecriture -> 201 created
2 si la ressource existe et quel est possible

	-> se referer au schema

ETAPE 3 "generer la reponse"
soit apres une erreur (exception) soit avec le contenue (text html ou binaire image)

Response line:
+ code de statut trouve (200 ok, 404 not found)

Generer Body:
-> si erreur chercher error_page du bloc config
	sinon generer html d'erreur standard

-> si succes le body prend tout le contenu binaire/texte lu et stocke
	
Content-Length: <taille_du_body>
Content-Type: text/html (ou image/png selon l'extension du fichier demandé).
Connection: keep-alive (ou close).

serialize()
methode met tout ca dans un string et la termine par \r\n

mettre ce texte dans le _write_buffer 	qui va dire a poll() qu'il est pret a envoyer 
sur le reseau



-> definir uri
uniform ressource identifier
->chercher quel bloc location de la config
->verifier sur la methode est accepte dans cette location
->mettre en place le vrai chemin
