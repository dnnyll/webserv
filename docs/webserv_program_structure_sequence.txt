━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SEQUENTIAL TIMELINE — COMPLETE OBJECT LIFECYCLE (FIXED)
poll() + HTTP/1.0 WEBSERVER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1 — PROGRAM STARTUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

main()
│
├── CREATE ConfigParser
│       purpose:
│           parse .conf file
│
│       contains:
│           - tokenizer
│           - parser cursor
│           - temporary token stream
│           - parsing state
│
│
├── CREATE ServerConfig
│       purpose:
│           represent one server block
│
│       contains:
│           - server_name
│           - host
│           - port list
│           - root directory
│           - error pages
│           - client_max_body_size
│           - vector<LocationConfig>
│
│
├── CREATE ListenEntry
│       purpose:
│           represent one listen directive
│
│       contains:
│           - ip
│           - port
│
│
├── CREATE LocationConfig
│       purpose:
│           represent one location block
│
│       contains:
│           - route path
│           - allowed methods
│           - root / alias
│           - index files
│           - autoindex flag
│           - upload directory
│           - CGI extension
│           - redirect rules
│
│
├── DESTROY ConfigParser
│
▼
CONFIGURATION READY

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2 — SERVER INITIALIZATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

main()
│
├── CREATE Reactor
│       purpose:
│           central poll() manager
│
│       contains:
│           - vector<pollfd>
│           - fd → handler map
│           - running flag
│           - deferred remove list
│
│
├── CREATE AcceptHandler (one per listen socket)
│       purpose:
│           manage listening socket
│
│       contains:
│           - listening fd
│           - ip / port
│           - Reactor reference
│           - ServerConfig reference
│
│
├── socket()
├── bind()
├── listen()
├── set nonblocking
│
├── ADD listening fd to Reactor (POLLIN)
│
▼
SERVER READY

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3 — EVENT LOOP STARTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

reactor.run()
│
▼
while (running)
│
├── poll(pollfds)
├── iterate active fds
├── lookup handler
│
└── dispatch:
POLLIN  → handle_input()
POLLOUT → handle_output()
ERROR   → handle_close()

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4 — NEW CLIENT CONNECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

poll()
│
├── listening fd → POLLIN
│
▼
AcceptHandler::handle_input()
│
├── accept()
├── set nonblocking(clientFd)
│
├── CREATE ClientHandler
│       purpose:
│           manage one HTTP/1.0 connection
│
│       contains:
│           - client socket fd
│           - client address
│           - connection state
│           - ReadBuffer
│           - WriteBuffer
│           - HttpRequest
│           - HttpResponse
│           - IRequestHandler*
│
│
├── CREATE ReadBuffer
│       purpose:
│           store incoming bytes
│
│
├── CREATE WriteBuffer
│       purpose:
│           store outgoing bytes
│
│
├── CREATE HttpRequest
│       purpose:
│           incremental HTTP parsing
│
│       contains:
│           - method
│           - URI
│           - version
│           - headers
│           - body
│           - ParseState
│
│
├── CREATE HttpResponse
│       purpose:
│           outgoing HTTP response
│
│       contains:
│           - status code
│           - headers
│           - body
│
│
├── ADD client fd to Reactor (POLLIN)
│
▼
BACK TO EVENT LOOP

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5 — REQUEST READING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

poll()
│
├── client fd → POLLIN
│
▼
ClientHandler::handle_input()
│
├── recv()
├── append to ReadBuffer
├── HttpRequest.feed()
│
├── ParseState:
│       REQUEST_LINE → HEADERS → BODY → COMPLETE
│
├── if incomplete:
│       return to poll()
│
▼
REQUEST COMPLETE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6 — ROUTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Router (stateless utility)
│
├── match ServerConfig
├── match LocationConfig
├── validate method
├── detect request type
│
├───────────────────────────────────────────────┐
│ STATIC FILE                                  │
├───────────────────────────────────────────────┤
│ CREATE StaticFileHandler                     │
│       purpose: serve files                   │
│       contains: file fd / path / size       │
│                                               │
├───────────────────────────────────────────────┤
│ CGI                                          │
├───────────────────────────────────────────────┤
│ CREATE CGIHandler                            │
│       purpose: async execution               │
│       contains: pid / pipes / buffer         │
│                                               │
├───────────────────────────────────────────────┤
│ UPLOAD                                       │
├───────────────────────────────────────────────┤
│ CREATE UploadHandler                         │
│       purpose: store uploads                 │
│                                               │
├───────────────────────────────────────────────┤
│ DELETE                                       │
└───────────────────────────────────────────────┘
CREATE DeleteHandler
purpose:
remove filesystem resource

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7 — RESPONSE GENERATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IRequestHandler.execute()
│
├── process request
├── fill HttpResponse
├── serialize response
├── write to WriteBuffer
│
├── switch POLLIN → POLLOUT
│
▼
BACK TO LOOP

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8 — RESPONSE SENDING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

poll()
│
├── client fd → POLLOUT
│
▼
ClientHandler::handle_output()
│
├── send()
├── advance offset
│
├── if partial:
│       return to poll()
│
▼
RESPONSE COMPLETE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9 — CONNECTION CLOSE (HTTP/1.0)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ClientHandler::handle_close()
│
├── remove fd from Reactor
├── close(fd)
│
├── DESTROY IRequestHandler
├── DESTROY HttpRequest
├── DESTROY HttpResponse
├── DESTROY ReadBuffer
├── DESTROY WriteBuffer
├── DESTROY ClientHandler
│
▼
BACK TO LOOP

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OBJECT LIFETIME SUMMARY (FIXED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROGRAM LIFETIME
│
├── Reactor
├── AcceptHandler
├── ServerConfig
├── ListenEntry
└── LocationConfig

PER CONNECTION
│
├── ClientHandler
├── ReadBuffer
├── WriteBuffer
├── HttpRequest
├── HttpResponse
└── IRequestHandler

REQUEST TYPE OBJECTS
│
├── StaticFileHandler
├── CGIHandler
├── UploadHandler
└── DeleteHandler

TEMPORARY
│
├── ConfigParser
└── Router (stateless utility)
