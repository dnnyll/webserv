 STEP 4 — Dev B implements the real parser  (Dev B)
  ───────────────────────────────────────────────────
  HttpRequest::feed() — full state machine
      REQUEST_LINE parsing
      HEADERS parsing
      BODY parsing
      CHUNKED parsing

  milestone: curl sends a real GET, feed() reaches COMPLETE


  STEP 5 — Dev B implements the real Router  (Dev B)
  ───────────────────────────────────────────────────
  Router::route() — full implementation
      server block selection
      location matching
      method validation
      static / upload / delete / redirect / error / CGI signal

  milestone: curl GET /index.html returns correct file content