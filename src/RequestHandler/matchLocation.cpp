#include "RequestHandler.hpp"

void	RequestHandler::matchLocation()
{
	std::vector<Location>::iterator = _config.locations.begin();

	//trouver la location la plus longue qui correspond
	//_config 
		// -> locations (vector a parcourir) 
			//-> pour chaque vector check path
	//get location
	//get real root
	std::cout << "match location" << std::endl;
}
